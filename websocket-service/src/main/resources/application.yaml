spring:
  application:
    name: websocket-service
  data:
    redis:
      port: 6379
      host: localhost
      password: Redis123
server:
  port: 8082
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
